#!/bin/bash

set -e

# Get the directory where this script is located
SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"

# Run the container with copilot and any arguments
"$SCRIPT_DIR/run.sh" copilot "$@"
